import { PayloadMessagePipe } from './payload-message.pipe';

describe('PayloadMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new PayloadMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
